#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
     int n;
  scanf("%d", &n);

  if (n > 9 || n < 0)
  {
    printf("Invalid Input");
  } 
    else
    {
    int square = n * n;
    if (square % 10 == n)
    {
      printf("Automorphic Number");
    }
        else
        {
      printf("Not Automorphic Number");
    }
  }
    return 0;
}
